{% extends "base.html.twig" %}

{% block body %}
    Hello, it's {{ page }}

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}